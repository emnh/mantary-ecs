Prompt: 1 / 1Write a js function that displays a h1 hello world.
Answer: 1 /
1Sure, here's a JavaScript function that creates an h1 element and sets its text
content to "Hello World!" before appending it to the body of the HTML
document:javascriptCopy codefunction displayHelloWorld() {
  const heading =
document.createElement('h1');
  heading.textContent = 'Hello World!';
document.body.appendChild(heading);
}
You can call this function from anywhere
in your JavaScript code to display the "Hello World!" message. For example, you
could add a button to your HTML page with an onclick attribute that calls the
function:htmlCopy code<button onclick="displayHelloWorld()">Click
me!</button>
When the button is clicked, the displayHelloWorld function will be
executed and the "Hello World!" message will be displayed in an h1 element on
the page.