Prompt: 1 / 1Update drawPlatform to use game.world.addChild
Answer: 1 / 1Sure,
here's the updated implementation of drawPlatform that uses
me.game.world.addChild:javascriptCopy codefunction drawPlatform(container, x, y,
width, height, image) {
  const platform = new me.Entity(x, y, {
    width:
width,
    height: height,
    collidable: true,
    type:
me.collision.types.WORLD_SHAPE
  });

  platform.renderable = new me.Sprite(0,
0, {
    image: image,
  });

  container.addChild(platform);
me.game.world.addChild(platform);
}
In the updated implementation, drawPlatform
now adds the platform entity to both the specified container and me.game.world
using container.addChild and me.game.world.addChild, respectively.